#Script for cp all image in upload folder to other server

1. you have to give permission to the key for execute
   sudo chmod 777 linux-class.pem
   sudo chmod 777 scp_upload.sh or sudo chmod u+x scp_upload.sh
   Also you have to give permission to the folder on other server that you gone copy to.
2. To run the script make sure your script is insite the project with the key of it
   "./scp_allimg.sh"

  So the script will copy the image in upload folder and then scp to other server automaticly
